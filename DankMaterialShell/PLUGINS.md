# DMS Plugins (as of 2026-09-11)

Installed via DMS plugin registry (reinstallable from registry):

- appShortcut
- dankAudioVisualizer
- dankKDEConnect (+ .meta)
- discordVoice
- dmsScreenshot
- githubHeatmap (+ .meta)
- mediaPlayer

Custom / own repos (sources on GitHub if reinstall needed):

- agent-notch — see `DMS-qs-AIAGENT-panel`
- screenTimeTracker — see `dms-screentime-study-timer`
- usbDetect — see `dms-usb-detect`

Plugin enable/disable state: see `plugin_settings.json` in this folder.
